<UserControl x:Class="BrendensFuntimeApp.View.NibbleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrendensFuntimeApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Background="#FF0D0D20"
            CornerRadius="5"
            Margin="3">
        <Grid>
            <ItemsControl ItemsSource="{Binding Bits}"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          Margin="3,3,3,20">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:BitView/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <TextBlock Text="{Binding StartingBitPosition}"
                       Foreground="White"
                       FontSize="12"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Right"
                       Margin="5"/>
        </Grid>
    </Border>
</UserControl>
